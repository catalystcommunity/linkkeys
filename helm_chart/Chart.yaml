apiVersion: v2
name: linkkeys
description: LinkKeys identity protocol server
type: application

# Chart version — updated automatically by release workflow
version: 0.4.2

# Application version — matches the project release version
appVersion: "0.4.2"
