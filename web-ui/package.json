{
  "name": "linkkeys-web-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run check:password-forms && npm test && tsc --noEmit && vite build",
    "test": "vitest run",
    "check:password-forms": "node scripts/check-password-manager-forms.mjs"
  },
  "dependencies": {
    "solid-js": "^1.9.5"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vite-plugin-solid": "^2.11.2",
    "vitest": "^3.2.7"
  }
}
