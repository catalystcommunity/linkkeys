[package]
name = "liblinkkeys"
version = "0.1.0"
edition = "2021"

[dependencies]
# CBOR serialization
ciborium = "0.2"
serde = { version = "1.0", features = ["derive"] }

# UUID support
uuid = { version = "1.10", features = ["v7", "serde"] }

# Utilities
log = "0.4"
