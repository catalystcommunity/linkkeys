/* Generated typed RPC client call-sites. */
/* Code generated by csilgen; DO NOT EDIT. */
#ifndef CSILGEN_CLIENT_GEN_H
#define CSILGEN_CLIENT_GEN_H

#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include "types.gen.h"
#include "codec.gen.h"

/* CsilgenTransport is the caller-supplied byte carrier: it performs the call named
 * by (service, op) with the already-encoded req bytes (CBOR over HTTP, say) and
 * writes the response bytes into *resp (the generated client frees them),
 * returning 0 on success or a non-zero error. */
typedef struct CsilgenTransport {
    int (*call)(void *self, const char *service, const char *op,
                const uint8_t *req, size_t req_len,
                uint8_t **resp, size_t *resp_len);
    void *self;
} CsilgenTransport;

/* Invoke Ops/healthcheck with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_ops_healthcheck(const CsilgenTransport *t, const EmptyRequest *req,
                        CheckResult *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Ops", "healthcheck", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CheckResult(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Ops/readiness with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_ops_readiness(const CsilgenTransport *t, const EmptyRequest *req,
                        CheckResult *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Ops", "readiness", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CheckResult(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Hello/hello with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_hello_hello(const CsilgenTransport *t, const HelloRequest *req,
                        HelloResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_HelloRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Hello", "hello", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_HelloResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Guestbook/create-entry with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_guestbook_create_entry(const CsilgenTransport *t, const CreateGuestbookRequest *req,
                        GuestbookEntry *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_CreateGuestbookRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Guestbook", "create-entry", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GuestbookEntry(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Guestbook/list-entries with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_guestbook_list_entries(const CsilgenTransport *t, const GuestbookListRequest *req,
                        GuestbookListResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GuestbookListRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Guestbook", "list-entries", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GuestbookListResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Guestbook/update-entry with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_guestbook_update_entry(const CsilgenTransport *t, const UpdateGuestbookRequest *req,
                        GuestbookEntry *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_UpdateGuestbookRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Guestbook", "update-entry", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GuestbookEntry(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Guestbook/delete-entry with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_guestbook_delete_entry(const CsilgenTransport *t, const DeleteGuestbookRequest *req,
                        DeleteGuestbookResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_DeleteGuestbookRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Guestbook", "delete-entry", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_DeleteGuestbookResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke DomainKeys/get-domain-keys with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_domain_keys_get_domain_keys(const CsilgenTransport *t, const EmptyRequest *req,
                        GetDomainKeysResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "DomainKeys", "get-domain-keys", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetDomainKeysResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke DomainKeys/get-revocations with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_domain_keys_get_revocations(const CsilgenTransport *t, const GetRevocationsRequest *req,
                        GetRevocationsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetRevocationsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "DomainKeys", "get-revocations", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetRevocationsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke UserKeys/get-user-keys with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_user_keys_get_user_keys(const CsilgenTransport *t, const GetUserKeysRequest *req,
                        GetUserKeysResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetUserKeysRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "UserKeys", "get-user-keys", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetUserKeysResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke ApplicationKeys/get-application-keys with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_application_keys_get_application_keys(const CsilgenTransport *t, const GetApplicationKeysRequest *req,
                        GetApplicationKeysResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetApplicationKeysRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "ApplicationKeys", "get-application-keys", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetApplicationKeysResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke ApplicationKeys/start-key-challenge with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_application_keys_start_key_challenge(const CsilgenTransport *t, const StartApplicationKeyChallengeRequest *req,
                        StartApplicationKeyChallengeResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_StartApplicationKeyChallengeRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "ApplicationKeys", "start-key-challenge", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_StartApplicationKeyChallengeResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke ApplicationKeys/add-key with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_application_keys_add_key(const CsilgenTransport *t, const AddApplicationKeyRequest *req,
                        AddApplicationKeyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_AddApplicationKeyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "ApplicationKeys", "add-key", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_AddApplicationKeyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke ApplicationKeys/renew-attestation with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_application_keys_renew_attestation(const CsilgenTransport *t, const RenewApplicationKeyAttestationRequest *req,
                        RenewApplicationKeyAttestationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RenewApplicationKeyAttestationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "ApplicationKeys", "renew-attestation", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RenewApplicationKeyAttestationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke ApplicationKeys/revoke-key with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_application_keys_revoke_key(const CsilgenTransport *t, const RevokeApplicationKeyRequest *req,
                        RevokeApplicationKeyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RevokeApplicationKeyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "ApplicationKeys", "revoke-key", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RevokeApplicationKeyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Identity/get-user-info with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_identity_get_user_info(const CsilgenTransport *t, const SignedUserInfoRequest *req,
                        UserInfo *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SignedUserInfoRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Identity", "get-user-info", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_UserInfo(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Handshake/handshake with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_handshake_handshake(const CsilgenTransport *t, const HandshakeRequest *req,
                        HandshakeResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_HandshakeRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Handshake", "handshake", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_HandshakeResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke I18n/get-translations with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_i18n_get_translations(const CsilgenTransport *t, const TranslationsRequest *req,
                        TranslationsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_TranslationsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "I18n", "get-translations", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_TranslationsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke I18n/list-locales with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_i18n_list_locales(const CsilgenTransport *t, const EmptyRequest *req,
                        ListLocalesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "I18n", "list-locales", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListLocalesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Ui/get-configuration with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_ui_get_configuration(const CsilgenTransport *t, const EmptyRequest *req,
                        GetUiConfigurationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Ui", "get-configuration", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetUiConfigurationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Notification/get-capabilities with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_notification_get_capabilities(const CsilgenTransport *t, const EmptyRequest *req,
                        GetNotificationCapabilitiesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Notification", "get-capabilities", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetNotificationCapabilitiesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Session/login-password with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_session_login_password(const CsilgenTransport *t, const SessionPasswordLoginRequest *req,
                        SessionPasswordLoginResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SessionPasswordLoginRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Session", "login-password", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SessionPasswordLoginResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Session/get-current with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_session_get_current(const CsilgenTransport *t, const EmptyRequest *req,
                        SessionCurrentResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Session", "get-current", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SessionCurrentResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Session/logout with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_session_logout(const CsilgenTransport *t, const EmptyRequest *req,
                        SessionLogoutResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Session", "logout", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SessionLogoutResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Session/introspect with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_session_introspect(const CsilgenTransport *t, const IntrospectBrowserSessionRequest *req,
                        IntrospectBrowserSessionResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_IntrospectBrowserSessionRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Session", "introspect", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_IntrospectBrowserSessionResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Recovery/request-password-recovery with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_recovery_request_password_recovery(const CsilgenTransport *t, const RequestPasswordRecoveryRequest *req,
                        RequestPasswordRecoveryResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RequestPasswordRecoveryRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Recovery", "request-password-recovery", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RequestPasswordRecoveryResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Recovery/validate-password-recovery with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_recovery_validate_password_recovery(const CsilgenTransport *t, const ValidatePasswordRecoveryRequest *req,
                        ValidatePasswordRecoveryResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ValidatePasswordRecoveryRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Recovery", "validate-password-recovery", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ValidatePasswordRecoveryResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Recovery/complete-password-recovery with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_recovery_complete_password_recovery(const CsilgenTransport *t, const CompletePasswordRecoveryRequest *req,
                        CompletePasswordRecoveryResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_CompletePasswordRecoveryRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Recovery", "complete-password-recovery", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CompletePasswordRecoveryResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke BrowserAuthorization/inspect with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_browser_authorization_inspect(const CsilgenTransport *t, const BrowserAuthorizationInspectRequest *req,
                        BrowserAuthorizationInspectResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_BrowserAuthorizationInspectRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "BrowserAuthorization", "inspect", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_BrowserAuthorizationInspectResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke BrowserAuthorization/complete with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_browser_authorization_complete(const CsilgenTransport *t, const BrowserAuthorizationCompleteRequest *req,
                        BrowserAuthorizationCompleteResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_BrowserAuthorizationCompleteRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "BrowserAuthorization", "complete", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_BrowserAuthorizationCompleteResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-users with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_users(const CsilgenTransport *t, const ListUsersRequest *req,
                        ListUsersResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ListUsersRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-users", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListUsersResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/get-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_get_user(const CsilgenTransport *t, const GetUserRequest *req,
                        GetUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "get-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/create-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_create_user(const CsilgenTransport *t, const CreateUserRequest *req,
                        CreateUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_CreateUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "create-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CreateUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/update-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_update_user(const CsilgenTransport *t, const UpdateUserRequest *req,
                        UpdateUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_UpdateUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "update-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_UpdateUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/deactivate-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_deactivate_user(const CsilgenTransport *t, const DeactivateUserRequest *req,
                        DeactivateUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_DeactivateUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "deactivate-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_DeactivateUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/activate-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_activate_user(const CsilgenTransport *t, const ActivateUserRequest *req,
                        ActivateUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ActivateUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "activate-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ActivateUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/purge-user with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_purge_user(const CsilgenTransport *t, const PurgeUserRequest *req,
                        PurgeUserResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_PurgeUserRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "purge-user", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_PurgeUserResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/revoke-domain-key with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_revoke_domain_key(const CsilgenTransport *t, const RevokeDomainKeyRequest *req,
                        RevokeDomainKeyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RevokeDomainKeyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "revoke-domain-key", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RevokeDomainKeyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/reset-password with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_reset_password(const CsilgenTransport *t, const ResetPasswordRequest *req,
                        ResetPasswordResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ResetPasswordRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "reset-password", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ResetPasswordResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/authenticate with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_authenticate(const CsilgenTransport *t, const AuthenticateRequest *req,
                        AuthenticateResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_AuthenticateRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "authenticate", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_AuthenticateResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-credential with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_credential(const CsilgenTransport *t, const RemoveCredentialRequest *req,
                        RemoveCredentialResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveCredentialRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-credential", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveCredentialResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_claim(const CsilgenTransport *t, const SetClaimRequest *req,
                        SetClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_claim(const CsilgenTransport *t, const RemoveClaimRequest *req,
                        RemoveClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-user-claims with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_user_claims(const CsilgenTransport *t, const ListUserClaimsRequest *req,
                        ListUserClaimsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ListUserClaimsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-user-claims", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListUserClaimsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/get-user-claims with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_get_user_claims(const CsilgenTransport *t, const AdminUserClaimsRequest *req,
                        AdminUserClaimsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_AdminUserClaimsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "get-user-claims", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_AdminUserClaimsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-user-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_user_claim(const CsilgenTransport *t, const SetUserClaimRequest *req,
                        SetUserClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetUserClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-user-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetUserClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-settable-policies with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_settable_policies(const CsilgenTransport *t, const EmptyRequest *req,
                        ListSettablePoliciesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-settable-policies", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListSettablePoliciesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-claim-types with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_claim_types(const CsilgenTransport *t, const EmptyRequest *req,
                        ListClaimTypesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-claim-types", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListClaimTypesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-claim-type with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_claim_type(const CsilgenTransport *t, const SetClaimTypeRequest *req,
                        SetClaimTypeResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetClaimTypeRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-claim-type", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetClaimTypeResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-claim-type with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_claim_type(const CsilgenTransport *t, const RemoveClaimTypeRequest *req,
                        RemoveClaimTypeResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveClaimTypeRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-claim-type", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveClaimTypeResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-claim-type-label with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_claim_type_label(const CsilgenTransport *t, const SetClaimTypeLabelRequest *req,
                        SetClaimTypeLabelResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetClaimTypeLabelRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-claim-type-label", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetClaimTypeLabelResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-claim-type-label with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_claim_type_label(const CsilgenTransport *t, const RemoveClaimTypeLabelRequest *req,
                        RemoveClaimTypeLabelResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveClaimTypeLabelRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-claim-type-label", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveClaimTypeLabelResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-trusted-issuers with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_trusted_issuers(const CsilgenTransport *t, const EmptyRequest *req,
                        ListTrustedIssuersResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-trusted-issuers", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListTrustedIssuersResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/add-trusted-issuer with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_add_trusted_issuer(const CsilgenTransport *t, const AddTrustedIssuerRequest *req,
                        AddTrustedIssuerResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_AddTrustedIssuerRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "add-trusted-issuer", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_AddTrustedIssuerResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-trusted-issuer with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_trusted_issuer(const CsilgenTransport *t, const RemoveTrustedIssuerRequest *req,
                        RemoveTrustedIssuerResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveTrustedIssuerRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-trusted-issuer", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveTrustedIssuerResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-release-rules with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_release_rules(const CsilgenTransport *t, const EmptyRequest *req,
                        ListReleaseRulesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-release-rules", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListReleaseRulesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-release-rule with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_release_rule(const CsilgenTransport *t, const SetReleaseRuleRequest *req,
                        SetReleaseRuleResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetReleaseRuleRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-release-rule", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetReleaseRuleResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-release-rule with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_release_rule(const CsilgenTransport *t, const RemoveReleaseRuleRequest *req,
                        RemoveReleaseRuleResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveReleaseRuleRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-release-rule", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveReleaseRuleResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-pending-claim-approvals with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_pending_claim_approvals(const CsilgenTransport *t, const EmptyRequest *req,
                        ListPendingClaimApprovalsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-pending-claim-approvals", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListPendingClaimApprovalsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/approve-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_approve_claim(const CsilgenTransport *t, const ApproveClaimRequest *req,
                        ApproveClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ApproveClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "approve-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ApproveClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/reject-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_reject_claim(const CsilgenTransport *t, const RejectClaimRequest *req,
                        RejectClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RejectClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "reject-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RejectClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/admin-issue-attestation with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_admin_issue_attestation(const CsilgenTransport *t, const AdminIssueAttestationRequest *req,
                        AdminIssueAttestationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_AdminIssueAttestationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "admin-issue-attestation", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_AdminIssueAttestationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/grant-relation with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_grant_relation(const CsilgenTransport *t, const GrantRelationRequest *req,
                        GrantRelationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GrantRelationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "grant-relation", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GrantRelationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/remove-relation with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_remove_relation(const CsilgenTransport *t, const RemoveRelationRequest *req,
                        RemoveRelationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveRelationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "remove-relation", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveRelationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-relations with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_relations(const CsilgenTransport *t, const ListRelationsRequest *req,
                        ListRelationsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ListRelationsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-relations", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListRelationsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/check-permission with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_check_permission(const CsilgenTransport *t, const CheckPermissionRequest *req,
                        CheckPermissionResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_CheckPermissionRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "check-permission", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CheckPermissionResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/recheck-pins with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_recheck_pins(const CsilgenTransport *t, const RecheckPinsRequest *req,
                        RecheckPinsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RecheckPinsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "recheck-pins", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RecheckPinsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/list-local-rps with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_list_local_rps(const CsilgenTransport *t, const ListLocalRpsRequest *req,
                        ListLocalRpsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ListLocalRpsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "list-local-rps", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListLocalRpsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/get-local-rp with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_get_local_rp(const CsilgenTransport *t, const GetLocalRpRequest *req,
                        GetLocalRpResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetLocalRpRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "get-local-rp", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetLocalRpResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/approve-local-rp with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_approve_local_rp(const CsilgenTransport *t, const ApproveLocalRpRequest *req,
                        ApproveLocalRpResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ApproveLocalRpRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "approve-local-rp", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ApproveLocalRpResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/deny-local-rp with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_deny_local_rp(const CsilgenTransport *t, const DenyLocalRpRequest *req,
                        DenyLocalRpResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_DenyLocalRpRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "deny-local-rp", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_DenyLocalRpResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/revoke-local-rp with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_revoke_local_rp(const CsilgenTransport *t, const RevokeLocalRpRequest *req,
                        RevokeLocalRpResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RevokeLocalRpRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "revoke-local-rp", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RevokeLocalRpResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/get-local-rp-policy with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_get_local_rp_policy(const CsilgenTransport *t, const GetLocalRpPolicyRequest *req,
                        GetLocalRpPolicyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_GetLocalRpPolicyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "get-local-rp-policy", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetLocalRpPolicyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/set-local-rp-policy with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_set_local_rp_policy(const CsilgenTransport *t, const SetLocalRpPolicyRequest *req,
                        SetLocalRpPolicyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetLocalRpPolicyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "set-local-rp-policy", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetLocalRpPolicyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Admin/purge-local-rp-tickets with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_admin_purge_local_rp_tickets(const CsilgenTransport *t, const PurgeLocalRpTicketsRequest *req,
                        PurgeLocalRpTicketsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_PurgeLocalRpTicketsRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Admin", "purge-local-rp-tickets", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_PurgeLocalRpTicketsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/change-password with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_change_password(const CsilgenTransport *t, const ChangePasswordRequest *req,
                        ChangePasswordResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ChangePasswordRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "change-password", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ChangePasswordResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/get-my-info with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_get_my_info(const CsilgenTransport *t, const EmptyRequest *req,
                        GetMyInfoResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "get-my-info", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_GetMyInfoResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/list-settable-policies with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_list_settable_policies(const CsilgenTransport *t, const EmptyRequest *req,
                        ListSettablePoliciesResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "list-settable-policies", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListSettablePoliciesResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/set-my-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_set_my_claim(const CsilgenTransport *t, const SetMyClaimRequest *req,
                        SetMyClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetMyClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "set-my-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetMyClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/remove-my-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_remove_my_claim(const CsilgenTransport *t, const RemoveMyClaimRequest *req,
                        RemoveMyClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RemoveMyClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "remove-my-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RemoveMyClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/set-my-claim-sharing with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_set_my_claim_sharing(const CsilgenTransport *t, const SetMyClaimSharingRequest *req,
                        SetMyClaimSharingResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SetMyClaimSharingRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "set-my-claim-sharing", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_SetMyClaimSharingResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/create-profile with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_create_profile(const CsilgenTransport *t, const CreateProfileRequest *req,
                        CreateProfileResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_CreateProfileRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "create-profile", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_CreateProfileResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/request-verification with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_request_verification(const CsilgenTransport *t, const RequestVerificationRequest *req,
                        RequestVerificationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RequestVerificationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "request-verification", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RequestVerificationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/list-verified-contact-methods with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_list_verified_contact_methods(const CsilgenTransport *t, const EmptyRequest *req,
                        ListVerifiedContactMethodsResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EmptyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "list-verified-contact-methods", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ListVerifiedContactMethodsResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/revoke-verified-contact-method with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_revoke_verified_contact_method(const CsilgenTransport *t, const RevokeVerifiedContactMethodRequest *req,
                        RevokeVerifiedContactMethodResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RevokeVerifiedContactMethodRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "revoke-verified-contact-method", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RevokeVerifiedContactMethodResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/request-contact-verification with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_request_contact_verification(const CsilgenTransport *t, const RequestContactVerificationRequest *req,
                        RequestContactVerificationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RequestContactVerificationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "request-contact-verification", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RequestContactVerificationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/confirm-contact-verification with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_confirm_contact_verification(const CsilgenTransport *t, const ConfirmContactVerificationRequest *req,
                        ConfirmContactVerificationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_ConfirmContactVerificationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "confirm-contact-verification", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_ConfirmContactVerificationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Account/enroll-application-instance with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_account_enroll_application_instance(const CsilgenTransport *t, const EnrollApplicationInstanceRequest *req,
                        EnrollApplicationInstanceResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_EnrollApplicationInstanceRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Account", "enroll-application-instance", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_EnrollApplicationInstanceResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Attestation/deposit-claim with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_attestation_deposit_claim(const CsilgenTransport *t, const DepositClaimRequest *req,
                        DepositClaimResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_DepositClaimRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Attestation", "deposit-claim", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_DepositClaimResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/sign-request with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_sign_request(const CsilgenTransport *t, const RpSignRequest *req,
                        RpSignResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpSignRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "sign-request", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpSignResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/decrypt-token with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_decrypt_token(const CsilgenTransport *t, const RpDecryptRequest *req,
                        RpDecryptResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpDecryptRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "decrypt-token", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpDecryptResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/verify-assertion with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_verify_assertion(const CsilgenTransport *t, const RpVerifyRequest *req,
                        RpVerifyResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpVerifyRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "verify-assertion", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpVerifyResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/userinfo-fetch with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_userinfo_fetch(const CsilgenTransport *t, const RpUserInfoRequest *req,
                        UserInfo *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpUserInfoRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "userinfo-fetch", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_UserInfo(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/issue-attestation with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_issue_attestation(const CsilgenTransport *t, const RpIssueAttestationRequest *req,
                        RpIssueAttestationResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpIssueAttestationRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "issue-attestation", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpIssueAttestationResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/resolve-domain-keys with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_resolve_domain_keys(const CsilgenTransport *t, const RpResolveDomainKeysRequest *req,
                        RpResolveDomainKeysResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpResolveDomainKeysRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "resolve-domain-keys", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpResolveDomainKeysResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke Rp/resolve-application-keys with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_rp_resolve_application_keys(const CsilgenTransport *t, const RpResolveApplicationKeysRequest *req,
                        RpResolveApplicationKeysResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_RpResolveApplicationKeysRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "Rp", "resolve-application-keys", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_RpResolveApplicationKeysResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

/* Invoke LocalRp/redeem-claim-ticket with a typed request and decode the typed
 * response. *resp_owner holds the response's backing storage; free it once
 * with csil_codec_arena_free when done with *resp. Returns non-zero on failure. */
static inline int csil_local_rp_redeem_claim_ticket(const CsilgenTransport *t, const SignedLocalRpTicketRedemptionRequest *req,
                        LocalRpTicketRedemptionResponse *resp, CsilCodecArena **resp_owner) {
    uint8_t *csil_reqb = NULL;
    size_t csil_reqn = 0;
    if (csil_encode_SignedLocalRpTicketRedemptionRequest(req, &csil_reqb, &csil_reqn)) return -1;
    uint8_t *csil_respb = NULL;
    size_t csil_respn = 0;
    int csil_rc = t->call(t->self, "LocalRp", "redeem-claim-ticket", csil_reqb, csil_reqn, &csil_respb, &csil_respn);
    free(csil_reqb);
    if (csil_rc != 0) { free(csil_respb); return csil_rc; }
    int csil_drc = csil_decode_LocalRpTicketRedemptionResponse(csil_respb, csil_respn, resp, resp_owner);
    free(csil_respb);
    return csil_drc;
}

#endif /* CSILGEN_CLIENT_GEN_H */
