apiVersion: v1
kind: Service
metadata:
  name: {{ include "demoappsite.fullname" . }}
  labels:
    {{- include "demoappsite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "demoappsite.selectorLabels" . | nindent 4 }}
